'use client';
import Link from 'next/link'; import {useEffect,useState} from 'react';
export default function Confirmation(){const [order,setOrder]=useState(null);useEffect(()=>{try{setOrder(JSON.parse(sessionStorage.getItem('stylenest-last-order')||'null'))}catch{}} ,[]);return <main><section className="confirmation"><div className="confirmation-mark">✓</div><p className="eyebrow">ORDER CONFIRMED</p><h1>Thank you for your order</h1>{order?<><p>Your order <strong>{order.id}</strong> has been placed successfully.</p><div className="confirmation-card"><div><span>Total</span><strong>${order.total.toFixed(2)}</strong></div><div><span>Items</span><strong>{order.items.reduce((n,i)=>n+i.quantity,0)}</strong></div><div><span>Status</span><strong>{order.status}</strong></div></div></>:<p>Your order details are available in My Account.</p>}<div className="confirmation-actions"><Link href="/account/orders" className="btn">View My Orders</Link><Link href="/" className="outline-btn">Continue Shopping</Link></div></section></main>}
