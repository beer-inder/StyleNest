'use client';
import Link from 'next/link'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import {useStore} from '../../../components/StoreProvider';
export default function Login(){const {login,user}=useStore();const router=useRouter();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');if(user)return <main><div className="auth-card"><p className="eyebrow">ALREADY SIGNED IN</p><h1>Welcome back, {user.name.split(' ')[0]}</h1><Link href="/account" className="btn">Go to My Account</Link></div></main>;const submit=e=>{e.preventDefault();const r=login(email,password);if(r.ok)router.push('/account');else setError(r.error)};return <main><div className="breadcrumb"><Link href="/">Home</Link> / Sign In</div><section className="auth-card"><p className="eyebrow">WELCOME BACK</p><h1>Sign in to StyleNest</h1><p className="auth-intro">Access your wishlist, orders and saved account details.</p>{error&&<div className="form-error">{error}</div>}<form onSubmit={submit} className="auth-form"><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label>Password<input type="password" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="Your password"/></label><button className="add-large">SIGN IN</button></form><p className="auth-switch">New to StyleNest? <Link href="/account/register">Create an account</Link></p></section></main>}
