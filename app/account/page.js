'use client';
import Link from 'next/link'; import {useStore} from '../../components/StoreProvider'; import ProductCard from '../../components/ProductCard';
export default function Account(){const {wishlist}=useStore();return <main><div className="breadcrumb"><Link href="/">Home</Link> / My Account</div><section className="account-page"><div className="account-side"><div className="account-avatar">SN</div><h2>My Account</h2><p>Welcome to your StyleNest space.</p><a className="active">Wishlist</a><a>Orders</a><a>Profile</a><a>Addresses</a></div><div className="account-main"><p className="eyebrow">SAVED FOR LATER</p><h1>My Wishlist</h1>{wishlist.length?<div className="product-grid">{wishlist.map(p=><ProductCard product={p} key={p.slug}/>)}</div>:<div className="empty"><p>Your wishlist is empty. Save pieces you love while browsing.</p><Link href="/" className="btn">Explore Collection</Link></div>}</div></section></main>}
