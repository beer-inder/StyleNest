'use client';
import Link from 'next/link'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import {useStore} from '../../../components/StoreProvider';
export default function Register(){const {register,user}=useStore();const router=useRouter();const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [error,setError]=useState('');if(user)return <main><div className="auth-card"><p className="eyebrow">ACCOUNT</p><h1>You are already signed in</h1><Link href="/account" className="btn">Go to My Account</Link></div></main>;const submit=e=>{e.preventDefault();if(password.length<6)return setError('Password must be at least 6 characters.');if(password!==confirm)return setError('Passwords do not match.');const r=register(name,email,password);if(r.ok)router.push('/account');else setError(r.error)};return <main><div className="breadcrumb"><Link href="/">Home</Link> / Create Account</div><section className="auth-card"><p className="eyebrow">JOIN STYLENEST</p><h1>Create your account</h1><p className="auth-intro">Save your wishlist and keep track of your orders.</p>{error&&<div className="form-error">{error}</div>}<form onSubmit={submit} className="auth-form"><label>Full name<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Jane Smith"/></label><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label>Password<input type="password" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="At least 6 characters"/></label><label>Confirm password<input type="password" required value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Repeat your password"/></label><button className="add-large">CREATE ACCOUNT</button></form><p className="auth-switch">Already have an account? <Link href="/account/login">Sign in</Link></p></section></main>}
